# Firebase Configuration
# Get these values from Firebase Console > Project Settings > General > Your apps > SDK setup and configuration

# Firebase API Key
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here

# Firebase Auth Domain (usually your-project-id.firebaseapp.com)
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com

# Firebase Project ID
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id

# Firebase Storage Bucket (usually your-project-id.appspot.com)
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Firebase Messaging Sender ID
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id

# Firebase App ID
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Firebase Measurement ID (for Analytics)
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id

# Optional: Firebase Realtime Database URL (auto-generated if not provided)
# NEXT_PUBLIC_FIREBASE_DATABASE_URL=https://your-project-id-default-rtdb.firebaseio.com

# Development Settings
# Set to 'development' to disable PWA features during development
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace all placeholder values with your actual Firebase config values
# 3. Never commit .env.local to version control (it's in .gitignore)
# 4. For production deployment (Vercel), add these as environment variables in the dashboard
